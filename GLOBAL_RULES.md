# 全局规则（跨项目生效）

> 本文件为 TraeWork **全局规则**的内容来源，适用于所有项目。
> 请将以下内容复制到：**设置 → 规则 → 创建** 中保存，即可跨项目生效。
> 项目级特定规则（技术栈版本、目录结构、主题色等）请在各项目的 `AGENTS.md` 中定义。

---

## 一、语言与沟通

- **所有回答必须使用中文表述**，包括解释、说明、代码注释等。
- 技术术语可保留英文（如 React、Tailwind、useState），但需配合中文上下文。
- 与用户沟通时保持专业、简洁、有帮助的语气，避免过度废话。

---

## 二、代码注释

- **关键逻辑和可能造成理解困难的部分必须添加简明的中文注释**，说明「为什么」而非「做什么」。
- 显而易见的代码（如简单赋值、JSX 结构）无需冗余注释。
- useEffect 的依赖列表特殊调整、DOM 操作、性能优化、边界处理等应说明原因。

---

## 三、代码抽象

- 当生成的代码超过 **20 行** 时，优先考虑是否可以进行适当的抽象或聚合：
  - 提取可复用的工具函数到 `utils/` 目录（目录不存在时按需创建）。
  - 拆分过长组件为子组件或自定义 Hook（命名为 `useXxx`）。
  - 将重复的 JSX 片段提取为独立的渲染函数或组件。
- 抽象的目标是**可读性 + 可维护性**，避免为了抽象而抽象导致过度工程化。

---

## 四、通用编码原则

- 避免多层嵌套，**提前返回**（Guard Clauses）减少缩进层级。
- 避免不必要的对象复制或克隆；大列表渲染注意性能。
- 使用严格比较 `===` / `!==`，禁止隐式类型转换。
- 条件渲染避免 `0 && <Component />` 陷阱（当条件为数字 0 时会渲染 `0`），使用 `condition ? <Component /> : null`。

---

## 五、React 编码规范

### 5.1 组件模式
- **仅使用函数组件 + Hooks**，禁止 Class 组件。
- 组件结构顺序（由上至下，保持一致）：
  1. import 语句
  2. 顶层常量（UPPER_SNAKE_CASE）
  3. 默认导出的主组件函数
  4. state 声明（useState）
  5. 自定义 Hook 调用
  6. useEffect（按执行逻辑排序，清理函数必须写）
  7. 事件处理函数（`handleXxx` / `onXxx` 命名）
  8. 辅助渲染函数（`renderXxx`）
  9. return JSX

### 5.2 Hooks 使用
- **useState**：相关状态可合并为对象，避免过度碎片化；但语义明显不同的状态应分开。
- **useEffect**：
  - 每个 effect 只做一件事，多个 effect 分离。
  - **必须提供清理函数**（如事件监听、定时器、订阅），返回 `() => cleanup()`。
  - 依赖列表要完整，且明确说明被省略依赖的原因（如仅需初始化时写注释 `// eslint-disable-next-line react-hooks/exhaustive-deps` 并说明）。
- **自定义 Hook**：当状态逻辑跨越 2+ 组件或单个组件内逻辑超过 30 行时，提取为自定义 Hook。

### 5.3 事件与性能
- 事件处理使用 `handle` 前缀命名：`handleSubmit`、`handleScroll`。
- 列表渲染必须提供稳定的 `key`，**禁止使用 index 作为 key**（除非列表静态且不会重排）。
- 避免在 render 中创建新对象/数组/函数作为 prop 传递（可能引发不必要的重渲染），除非是轻量级或被 memo 组件消费。
- `window.addEventListener` 使用 `{ passive: true }` 优化滚动类监听。

---

## 六、样式与 Tailwind 通用规范

### 6.1 样式方案选择
- **优先使用 Tailwind CSS 工具类**处理：布局（flex/grid）、间距、颜色、圆角、阴影、响应式断点。
- **使用独立 CSS 文件**处理以下场景：
  - 复杂选择器（`:hover > .child`、`+` 相邻兄弟、属性选择器等）
  - `@keyframes` 动画定义（大量自定义动画）
  - 伪元素 `::before` / `::after` 大量装饰
  - Tailwind 无法表达的媒体查询或 @layer 规则

### 6.2 Tailwind 使用规范
- 自定义颜色/字体/动画**必须统一在 `tailwind.config.js` 的 `theme.extend` 中定义**，禁止在组件中硬编码 `hex` 值（除非一次性场景）。
- 新增样式时**优先复用**项目已在 `tailwind.config.js` 中定义的主题色与字体，具体项见各项目 `AGENTS.md`。

### 6.3 独立 CSS 规范
- 类名使用 **kebab-case**，避免与 Tailwind 类名冲突（可加项目前缀，如 `.card-`、`.hero-`）。
- 属性书写顺序：定位/盒模型 → 尺寸 → 排版 → 外观 → 动画/交互。
- 动画性能：优先动画 `transform` 和 `opacity`，避免动画 `width`/`height`/`top`/`left`（引发重排）。
- 使用 `will-change` 提示浏览器优化动画目标属性（避免滥用）。

### 6.4 响应式
- 所有页面必须**响应式**，至少适配：桌面（>1024px）、平板（768-1024）、手机（<600px）。

---

## 七、DOM 与安全

- 操作原生 DOM 时（`querySelector`、`addEventListener`）必须在 `useEffect` 内进行，并在清理函数中移除。
- 图片必须提供 `alt` 属性，装饰性图片 `alt=""`。
- 外链使用 `target="_blank"` 时必须加 `rel="noopener noreferrer"`。
- 禁止使用 `dangerouslySetInnerHTML`，除非用户明确要求且来源可信。

---

## 八、Git 提交规范

### 8.1 自动提交
- **每次完成新的功能开发或规则修改后，自动提交一次 Git 版本**。
- 提交信息格式（中文，语义化）：
  ```
  feat: 新增 <功能简述>
  fix: 修复 <问题简述>
  style: 调整 <样式/格式 简述>
  refactor: 重构 <模块/组件 简述>
  chore: 配置 <规则/工具 简述>
  docs: 更新 <文档/注释 简述>
  ```
- 示例：`feat: 新增记账工具收入分类功能`、`chore: 添加项目级 AGENTS.md 规则文件`

### 8.2 提交粒度
- 小步提交，一次提交只做一件事，便于回溯。
- 禁止把大量不相关的改动塞进一次提交。

---

## 九、任务执行流程（AI 自检清单）

每次接到任务后，按以下步骤执行：

1. **理解需求**：复述任务关键点，有歧义立即询问用户，不猜测。
2. **探索上下文**：先查阅相关文件与代码风格，再动手修改。
3. **制定计划**：复杂任务使用 TodoWrite 拆分步骤。
4. **执行修改**：遵循以上规则编写代码，中文注释关键逻辑。
5. **自我验证**：
   - 代码超过 20 行？→ 考虑抽象/聚合。
   - 关键逻辑？→ 加中文注释。
   - 使用已定义主题色/字体？→ 检查项目 `tailwind.config.js` 与 `AGENTS.md`。
   - useEffect 有清理函数？→ 检查事件监听器/定时器。
   - 列表有稳定 key？→ 不用 index。
6. **运行验证**：执行项目构建命令确保通过（若修改涉及代码）。
7. **Git 提交**：功能完成后，按 §8 规范自动提交。
8. **交付说明**：用中文说明改动内容、影响范围、如何验证，并附上文件链接。
